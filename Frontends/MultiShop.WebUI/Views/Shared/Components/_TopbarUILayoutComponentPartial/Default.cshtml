<div class="topbar-announcement">
    <div class="topbar-announcement-inner px-xl-5">
        <div class="topbar-spacer"></div>

        <p class="topbar-message">
            Free standard shipping on orders over 3.500 TL
        </p>

        <nav class="topbar-links" aria-label="Utility links">
            <a href="/Contact/Index/">Help</a>
            <span aria-hidden="true"></span>
            <a href="/Order/Index">Order Tracking</a>
            <span aria-hidden="true"></span>
            <a href="/Default/Index/">Stores</a>
        </nav>
    </div>
</div>

<style>
    .topbar-announcement {
        min-height: 40px;
        color: #111111;
        background: #ffffff;
        border-bottom: 1px solid #ececec;
    }

    .topbar-announcement-inner {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        align-items: center;
        min-height: 40px;
        padding-right: 24px;
        padding-left: 24px;
        gap: 18px;
    }

    .topbar-message {
        margin: 0;
        color: #111111;
        font-size: 13px;
        font-weight: 700;
        letter-spacing: 0.02em;
        line-height: 1.3;
        text-align: center;
        white-space: nowrap;
    }

    .topbar-links {
        display: inline-flex;
        align-items: center;
        justify-content: flex-end;
        gap: 10px;
        min-width: 0;
    }

    .topbar-links a {
        color: rgba(17, 17, 17, 0.78);
        font-size: 12px;
        font-weight: 600;
        line-height: 1;
        text-decoration: none;
        white-space: nowrap;
        transition: color 0.18s ease, opacity 0.18s ease;
    }

    .topbar-links a:hover,
    .topbar-links a:focus {
        color: #000000;
        text-decoration: none;
    }

    .topbar-links span {
        width: 1px;
        height: 12px;
        background: rgba(0, 0, 0, 0.14);
    }

    @@media (max-width: 767.98px) {
        .topbar-announcement-inner {
            grid-template-columns: 1fr;
            min-height: 38px;
            padding-right: 14px;
            padding-left: 14px;
        }

        .topbar-spacer,
        .topbar-links {
            display: none;
        }

        .topbar-message {
            font-size: 12px;
            white-space: normal;
        }
    }
</style>
